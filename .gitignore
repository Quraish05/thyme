# OS
.DS_Store

# Editor / tooling
.claude/settings.local.json
.idea/
.vscode/

# Env
.env
.env.*
!.env.example

# Logs
*.log

# Demo narration script — a working draft, not part of the docs set yet.
docs/demo-script.md

# Stray Next.js build output if a build is ever run from the repo root
# (the real app builds under frontend/.next, ignored by frontend/.gitignore).
/.next/

# Note: frontend/ and backend/ each have their own .gitignore
# covering node_modules, .next, .venv, caches, etc.
